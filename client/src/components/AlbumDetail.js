// @flow
import React from 'react';
import { Text, View, StyleSheet, Img} from 'react-native';

const AlbumDetail = props => {
  return (
    <View>
      <Text>{props.title}</Text>
    </View>
  );
};

export default AlbumDetail;
